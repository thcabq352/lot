[package]
name = "lot-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "lot mcp — stdio MCP server (native agent door)."

[dependencies]
lot-core = { workspace = true }
serde_json = { workspace = true }
